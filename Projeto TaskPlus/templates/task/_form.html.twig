{# templates/task/_form.html.twig #}
{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.dueDate) }}
    {{ form_row(form.isCompleted) }}
    {{ form_row(form.taskGroups) }} {# Adicionado o campo para vincular a grupos #}

    <button class="btn btn-primary">{{ button_label|default('Salvar') }}</button>
{{ form_end(form) }}